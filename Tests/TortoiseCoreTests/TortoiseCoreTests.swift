import Testing
@testable import TortoiseCore

@Suite("TortoiseCore")
struct TortoiseCoreTests {
    @Test("placeholder — replace with real tests in Phase 1")
    func placeholder() {
        #expect(true)
    }
}
