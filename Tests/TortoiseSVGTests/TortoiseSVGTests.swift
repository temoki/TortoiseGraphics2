import Testing
@testable import TortoiseSVG

@Suite("TortoiseSVG")
struct TortoiseSVGTests {
    @Test("placeholder — replace with real tests in Phase 3")
    func placeholder() {
        #expect(true)
    }
}
